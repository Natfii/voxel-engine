[World]
# Seed for terrain generation (integer)
seed = 1124345

# World size in chunks (width x height x depth)
# Each chunk is 16x16x16 blocks, each block is 0.5 units
world_width = 12
world_height = 3
world_depth = 12
