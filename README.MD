# Voxel Engine

A Classicube inspired project.

### Prerequisites

- C++17 compatible compiler
- CMake 3.10 or higher

### TODO

Change how blocks are handled. ✅ 

Create a dedicated Player class to handle user input.

Main Menu.

🛠️ Build Instructions 🛠️

🧪 One-Time Setup
1. Open Terminal / Developer Command Prompt and navigate to the project root (voxel-engine1/).

2. Create a build folder
    mkdir build
    cd build

3. Run CMake configuration
    cmake ..

4. Build
    cmake --build . --config

5. This builds the voxel-engine.exe and places it in "build/Release/voxel-engine.exe". 



